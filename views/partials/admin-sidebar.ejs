<nav id="sidebar">
    <div class="sidebar-header">
        <h3>MVSM Admin</h3>
    </div>

    <ul class="list-unstyled components">
        <li class="<%= title === 'Admin Dashboard' ? 'active' : '' %>">
            <a href="/admin/dashboard"><i class="fas fa-tachometer-alt me-2"></i> Dashboard</a>
        </li>
        <li class="<%= title === 'Manage Users' ? 'active' : '' %>">
            <a href="/admin/users"><i class="fas fa-users me-2"></i> Users</a>
        </li>
        <li class="<%= title === 'Manage Businesses' ? 'active' : '' %>">
            <a href="/admin/businesses"><i class="fas fa-store me-2"></i> Businesses</a>
        </li>
        <li class="<%= title === 'Manage Reports' ? 'active' : '' %>">
            <a href="/admin/reports" class="d-flex justify-content-between align-items-center">
                <span><i class="fas fa-flag me-2"></i> Reports</span>
                <% if (typeof pendingReportCount !== 'undefined' && pendingReportCount > 0) { %>
                    <span class="badge bg-danger rounded-pill"><%= pendingReportCount %></span>
                <% } %>
            </a>
        </li>
        <li>
            <a href="/"><i class="fas fa-home me-2"></i> Main Site</a>
        </li>
        <li>
            <a href="/users/logout" class="text-danger"><i class="fas fa-sign-out-alt me-2"></i> Logout</a>
        </li>
    </ul>
</nav>